---

- name: "Dotfile folder"
  stat:
    path: "{{ dotfiles_folder }}"
  register: dotfile_folder

- name: "Cloning dotfiles"
  git:
    repo: "{{ dotfiles_repo }}"
    dest: "{{ dotfiles_folder }}"
  when: not dotfile_folder.stat.exists
